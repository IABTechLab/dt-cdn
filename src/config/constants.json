/*
* Constants and IDs that do not change.
* These have been extracted from the general config.
* We do not use the const keyword because at time of writing (2018) not
* all browsers natively support the keyword. Browserfy does not transform
* to rewrite this to a var or let at this time.
*/
{
  /* ID values used when building the DigiTrust framework DOM objects */
  "consentLinkId": "digitrust-optout",
  /* dt common frame name */
  "locatorFrameName": "__dtLocator"
}
